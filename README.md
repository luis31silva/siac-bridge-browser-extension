# siac-bridge-browser-extension
Browser extension that captures the authentication token from portal.siac.pt (Portuguese pet identification system) and exposes it to authorized web applications via a secure messaging bridge.
